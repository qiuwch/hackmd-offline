# How to generate a dataset (COCO format)

###### tags: `unrealcv`

The COCO API, definition. 

Where to get UE4 scenes.

Generate images and ground truth from unrealcv.

Use tensorflow to train an object detector.